# 尖塔助手 Windows 本地版

本地实时建议无需 API Key。适用 Windows 10/11 x64，支持铁甲战士、静默猎手、故障机器人。需要自己拥有 Slay the Spire 游戏。

## 给朋友的使用步骤

1. 在 Steam 安装 Slay the Spire。订阅 [ModTheSpire](https://steamcommunity.com/sharedfiles/filedetails/?id=1605060445) 和 [BaseMod](https://steamcommunity.com/sharedfiles/filedetails/?id=1605833019)，等待下载完成。
2. 将 ZIP **完整解压**到自己的可写文件夹，例如 `D:\游戏工具\尖塔 助手`。不要在 ZIP 内运行，也不要只复制 EXE。
3. 双击 `SpireAssistant.exe`。程序自动寻找 Steam 游戏目录；找不到时点“选择文件夹”，选择包含 `desktop-1.0.jar` 的文件夹。点“检查依赖”。也可手动选择两个 Mod 的 JAR。
4. 先退出已有游戏。点击“启动实时顾问”，确认后安装通信 Mod 并启动游戏。旧 Mod 会备份，不会覆盖存档。
5. 手动开始或继续一局。保持助手窗口可见，手动出牌、选择奖励、地图、商店、篝火或卡牌。助手显示推荐动作、目标、理由；你可以不采纳。
6. 切换模式前先手动退出游戏，再关闭并重新打开助手。助手不会强制终止游戏。

所有必要 Python 运行文件随包提供，不需要安装 Python、Git、JDK 或 Codex。构建用 .NET EXE 启动器依赖 Windows 自带的 .NET Framework 4.x。

## 三种入口

- **实时顾问**：通信 Mod 以固定只读模式启动，不启动 bridge 外部进程，不消费命令；Java 命令入口也拒绝所有请求。约每 100 ms 输出一次只读快照，界面每 100 ms 检查。状态变更、正在结算、文件损坏或超过 2.5 秒没有心跳会清除建议。计算结果再次核对状态身份，不能覆盖新状态。
- **原有自动游玩**：保留原来的 fast-policy-v5 流程及发布门禁。必须有与当前源码完全匹配、通过完整验证的静态发布凭据，否则显示原因并拒绝启动。本候选包已带与源码绑定的静态凭据；搬迁目录后仍可校验，不需要 Git。游戏内启动尚待实机验收，不绕过门禁。此模式会自动开始并操作对局，请仅在自己愿意时选择。
- **战报查看**：浏览器中打开仅绑定 `127.0.0.1` 的本地只读看板。新包没有历史战报，空列表是正常状态。

## 看建议更方便

主界面用左侧栏区分实时顾问、自动游玩和战报，右侧只呈现当前建议。动作大字显示，手牌位置单独标记；没有建议时隐藏目标与评分。点击“查看判断依据”展开详细理由。游戏路径与 Mod 配置集中在“游戏设置”独立窗口。

点击右上角“精简悬浮窗”可切换为较小的置顶窗口，便于边玩边看；点击“完整窗口”恢复并取消置顶。程序在创建窗口前启用 DPI 感知；本机 125% 缩放已验证，避免原版窗口被系统作为低分辨率图像放大。独占全屏游戏可能覆盖普通置顶窗口，可使用游戏的窗口或无边框显示模式。所有建议区域仅用于显示，仍在游戏中手动出牌。

## 可选远程模型

顾问模式固定使用本地策略。原自动模式已有可选 DeepSeek 宏观建议，默认关闭；高级使用者可按源码 `DEEPSEEK_MACRO.md` 配置自己的环境变量。启用远程模型会改变策略指纹，需要重新通过自动模式发布门禁。API Key 不随包提供，不会被顾问读取或用于联网。当前候选包不声称远程服务已实测。

## 数据与问题排查

- 设置和顾问临时状态位于 `data`；自动模式保持原来的 `app` 下战报目录。分享时使用原始 ZIP，不要将用过的目录重新打包。
- “缺少依赖”：完成上述订阅、Steam 下载和目录选择，再检查。不会替你下载游戏或订阅项目。
- “检测到游戏仍在运行”：自己退出现有游戏后重试，程序不会杀进程。
- “等待连接”：检查游戏是否通过本助手的顾问入口启动；旧通信 Mod/普通自动模式状态不被接纳。
- “游戏正在结算”：等待当前动画、出牌和敌方回合完成。
- “状态过期”：旧建议已撤下；检查游戏及 `data/advisor-game.log`，必要时手动退出并重新启动。
- 启动失败：检查 `data/assistant-error.txt`；确保完整解压且目录可写。
- 游戏卡牌和敌人名称沿用游戏导出的语言；部分策略依据保留英文代码以便诊断。建议是启发式判断，不承诺最优或胜率。

## 最短实机验收（尚需玩家参与）

请先明确授权此次游戏验证。最短流程：关闭已有游戏 → 解压候选包 → 启动顾问 → 手动开始铁甲战士一局 → 连续打两张牌（第二张可故意不采纳建议）→ 结束回合 → 选一次战斗奖励 → 选下一地图节点 → 退出游戏。

观察每次操作后旧建议是否撤下、目标是否对应当前手牌/敌人、无操作时程序是否保持不出牌不点击。游戏关闭后 2.5 秒内应显示断线且没有旧建议。商店、篝火、删牌/升级/弃牌选择需到达相应场景后另测；未到达的场景不能记为通过。自动游玩启动与实际模式切换也需另行授权，不能由顾问回放结果替代。

## 从源码构建

开发者准备 Windows CPython 3.12（含 Tk）、JDK 8 和本地游戏/Mod 编译依赖：

```powershell
python build_windows.py --python-home 'C:\Python312' --jdk 'C:\Java\jdk8' --game-dir 'D:\SteamLibrary\steamapps\common\SlayTheSpire'
```

在完整源码工作区运行上述构建（本机交付位置见进度文件）；运行包内附带运行源码供审阅，完整测试资料保留在源码工作区。构建会执行完整回归、逐文件校验发布源码与已测源码，并生成原自动模式静态凭据。输出位于 `dist` 的新时间戳目录和 ZIP；不会安装 Mod、启动游戏、复制密钥或打包个人游戏存档。默认构建以明确模块清单收集源码，附 Java 源码、C# 启动器和构建脚本。完整工作区回归：`python -m unittest discover -p 'test_*.py'`；顾问专项：`python -m unittest test_assistant -v`。

新安装自动模式的初始案例集仅包含 12 个固定合成回归案例（fixture- 标记），不包含任何个人对局。

当前构建为本地分享用途，不会上传创意工坊。
