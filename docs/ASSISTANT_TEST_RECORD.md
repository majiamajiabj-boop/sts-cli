# Windows 助手验收记录

日期：2026-09-21。当前发布：**SpireAssistant-Windows-20260921-230502**。离线验收及部分实机验收通过，目标尚未全部完成。

## 构建与离线验证

- 完整项目回归：2947 项通过；git diff --check 通过。证据：.tools/assistant-build-20260921-230502/。
- 原自动模式静态发布门禁 clear；逐文件验证发布源码与已测试源码一致。初始化仅包含 12 个明确 fixture- 标记的合成回归案例，无个人历史，不放宽原门禁。
- Steam 注册表、库清单、游戏清单、手动游戏/Mod 选择、中文与空格路径通过测试。实际 JDK 8 UTF-8 参数文件和 Properties/JSON 路径往返通过。
- 日志回放覆盖战斗、卡牌/首领奖励、商店、事件、网格与手牌选择；地图、篝火、金币奖励有单独合成用例。回放不替代实机验收。
- 状态序号/内容变化、忙碌、断线、过期、非法来源及迟到计算结果的作废检查通过。
- 顾问不导入控制器/桥接，不执行 Action.execute，不联网；实际构建 JAR 拒绝全部 12 类命令且阻断命令队列；跨进程模式锁通过。
- 实际 ModTheSpire 补丁注入器对两个 InputAction 补丁注入通过。旧包省略局部变量信息，实机曾报 Cannot determine name；新增测试先复现同一错误，再验证 -g:source,lines,vars 修复。
- 白名单发布，不含个人密钥、.env、个人日志、战报、存档或设置；包含第三方许可。游戏、游戏 Java、ModTheSpire、BaseMod 由使用者安装，不随包分发。

## EXE 与界面

- 原独立 EXE 验收在源码外中文/空格目录完成，PATH 仅 Windows，Python/Codex 环境变量指向不存在位置；实际使用包内 CPython 3.12.14 与 Tk 8.6.12。新界面保留同一 EXE 和运行环境。
- 原实际 EXE 缺失运行时显示中文错误；四类依赖缺失、只读空白战报服务均通过。战报首页、健康检查、对局列表 HTTP 200，mode=read_only，count=0。
- 新版 223415 实际 EXE 在源码外目录分别启动完整与精简窗口，动态输入检查通过：动作、目标、手牌标记及详情在忙碌状态一并清空，没有启动游戏。
- 已检查新版真实窗口截图：大字动作、独立目标条、手牌标记、折叠理由。源码 Tk 布局检查覆盖 980×780、760×640、440×510、380×440，主要内容未越界；缺失依赖和展开详情另有截图。
- 新版 EXE 已以 --compact --state-file 打开当前游戏的真实只读 MAP 快照，供玩家预览；没有重启游戏或替换当前加载的 Mod。
- 窗口工具初始化失败，采用只读窗口截图与应用自检。EXE 最终截图使用 PrintWindow 避免其他窗口遮挡；没有注入键鼠。早期测试脚本相对输出路径错误已修正，不计为产品通过证据。

## 已完成的实机项目

本次游戏由玩家从 221712 版真实 EXE 启动；223415 仅修改显示层，策略与 Java 源码未变。实机为已存在的 DEFECT 第 36 层对局，没有自主刷局。

- Mod 加载、连接和战斗建议：玩家确认正常；seq 21 的“核心电涌（手牌 3）→ 圆球行者（94 HP）”与真实快照和窗口截图一致。
- 手动出牌/结束回合刷新：玩家确认正常；记录了 busy/ready 转换和 seq 102 第 2 回合新建议“创造性 AI+（手牌 5）”。玩家停手期间状态保持稳定，没有自动出牌。
- 战斗奖励、卡牌奖励及地图：玩家明确确认正常；已捕获 COMBAT_REWARD seq 132–135、CARD_REWARD seq 134、MAP seq 136。
- 搜寻 GRID seq 51 已采集，采集状态回放能生成当前选牌建议；没有将回放认定为该界面的完整实机验收。
- 已核对安装目录中的 221712 CommunicationMod.jar 与当时通过测试的发布 JAR 哈希一致；运行命令含 advisor，只读模式下没有 bridge 或自动控制器。

## 尚未完成与最短后续步骤

- 商店、篝火、搜寻/弃牌/升级等卡牌选择界面完整操作验收。
- 实际退出游戏后的断线和旧建议清除。
- 自动游玩实际启动与模式切换需另行授权，不能以顾问结果替代。
- 另一台 Windows 电脑、其他 DPI/显示器、第三方角色/机制 Mod、远程 API 未验证。

玩家按自己的意愿继续，在到达商店或篝火时查看建议并操作；出现选牌界面时确认目标对应。结束测试时手动退出游戏，确认约 2.5 秒后没有旧建议。未到达或未观察的项目保持未验证。

## 已知限制

支持铁甲战士、静默猎手、故障机器人；观察者和第三方角色没有策略支持。每次建议使用新的策略对象，跨状态记忆可能不足。约 100 ms 采样/刷新，不承诺零延迟；文件被短暂占用时快照会在后续帧重试。建议不保证最优，卡牌和敌人名称沿用游戏语言，部分详细依据保留英文代码。精简置顶窗可能被独占全屏遮挡，可使用窗口或无边框模式。EXE 未代码签名。

## 证据位置

.tools/assistant-build-20260921-230502/（完整回归、源码绑定、静态门禁）；.tools/assistant-ui-v2-qa/（布局）；.tools/assistant-ui-v2-exe-qa/（实际 EXE 动态自检和截图）；.tools/assistant-live-verified/、.tools/assistant-live-evidence.jsonl（实机）；.tools/assistant-live-firstfailure/（旧包失败）。个人实机证据仅保留在源码工作区，不随分享 ZIP 分发。

## 暖深灰主题验收
224709 只调整显示配色，采用暖深灰底、暖白动作、琥珀手牌标记，绿色仅用于连接状态；按钮、输入框、滚动条与各状态使用同一配色表。2947 项完整回归及原静态门禁通过。实际 EXE 完整/精简窗口截图已检查，动态自检证明动作、目标、手牌标记和详情在过期时全部清空。证据：.tools/assistant-warm-exe-qa/。未因此增加游戏操作或把尚未验证的游戏场景计为通过。

## DPI 模糊诊断与布局重做
用户再次反馈外观和文字模糊。对现有真实 EXE 窗口只读检测得到 awareness=0、window_dpi=96、monitor_scale=125；原版未启用 DPI 感知。现改为在 Tk 创建任何窗口前调用 DPI 设置 API（[Microsoft 文档](https://learn.microsoft.com/en-us/windows/win32/hidpi/setting-the-default-dpi-awareness-for-a-process)）。源码窗口实测 awareness=2、DPI=120，布局以实际缩放绘制，未更改 Windows 显示设置。跨显示器切换仍未实测。

界面重做为侧栏与紧凑建议区，独立设置窗口，等待时隐藏空目标与详情，去掉大框与重复配置按钮。已在本机 125% 下检查等待、战斗、精简、小窗口、独立设置、展开详情和过期状态；修复小窗口展开详情后底部越界。最终实际 EXE 验证已完成，见下。

## 最终清晰版 EXE 验收
230502 完整 2947 项回归和原静态门禁通过。实际 EXE 在源码外中文/空格目录启动，完整与精简窗口均报告 awareness=2、DPI=120；与旧窗口 0/96 对比，已启用实际显示器 DPI 渲染。最终原始像素截图已检查。完整窗首次打开保持 1150×775 像素；修复了映射前自动调整尺寸导致缩到最小宽度的问题。动态状态验证中，结算时显示“等待回合结算”，动作、目标、手牌标记和详细依据全部作废。连接、计算、结算分别显示对应提示。证据：.tools/assistant-clean-exe-qa/。没有启动游戏；多显示器 DPI 切换仍未实测。

## 2026-09-22 仓库整理验证
78 个测试移至 tests/，6 份说明移至 docs/。更新路径和导入后完整回归 2948 项通过，实际 Java 测试依赖启用；新增回归确认 relocated tests/docs 仍纳入冻结检查，归档目录排除。完整构建 20260922-211516 成功，静态门禁 clear，发布内容白名单和源码一致性检查通过。构建产物仅留在本地验证目录，未作为新的用户版本发布。

73 项旧产物以恢复清单归档；归档预览、保护当前状态与最新包、实际移动/恢复及越界拒绝检查通过。逐文件核对确认无关既有源码修改完整保留，原 230502 分享 ZIP 的 SHA-256 仍为 62ff5e12dde765c5b813d986782869b563ceb4fe302d5413cdbdea8485bfa97e。本次未启动游戏，也未新增实机通过项。证据：.tools/repository-cleanup-20260922/、.tools/assistant-build-20260922-211516/。
